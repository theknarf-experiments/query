[package]
name = "datalog-eval"
version = "0.1.0"
edition = "2021"

[dependencies]
datalog-parser = { path = "../datalog-parser" }
datalog-planner = { path = "../datalog-planner" }
logical = { path = "../logical" }

[dev-dependencies]
internment = "0.8"
